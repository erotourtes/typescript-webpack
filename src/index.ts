import { MyModule } from './module'

MyModule.greet();

console.log(`
    1. tsc, npm init
    2. install dev dependencies
        "ts-loader"
        "typescript"
        "webpack"
        "webpack-cli"
        "webpack-dev-server"
        <npm i ts-loader -D>
    3. webpack.config.js
    4. .json scripts (build, server etc.);
`);
